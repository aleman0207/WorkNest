<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_gravity="center"
    android:id="@+id/pantallaInfo"
    tools:context=".pantallaInfoEmpleado">

    <!-- Fondo de imagen -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/fondo"
        android:scaleType="centerCrop">
    </ImageView>

    <!-- Capa de color con opacidad (cobertura) -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.7"
        android:background="@color/prueba"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center">

        <androidx.cardview.widget.CardView
            android:layout_width="400dp"
            android:layout_height="100dp"
            android:layout_marginBottom="30dp"/>

        <androidx.cardview.widget.CardView
            android:layout_width="400dp"
            android:layout_height="100dp"
            android:layout_marginBottom="50dp"/>

        <Button
            android:layout_width="280dp"
            android:layout_height="60dp"
            android:id="@+id/buttonFicharEntrada"
            android:layout_marginBottom="30dp"
            android:text="Fichar Entrada"
            android:textSize="20dp"
            android:fontFamily="@font/promptmedium"
            android:backgroundTint="@color/navy"
            app:cornerRadius="20dp"/>

        <Button
            android:layout_width="280dp"
            android:layout_height="60dp"
            android:id="@+id/buttonFicharSalida"
            android:text="Fichar Salida"
            android:textSize="20dp"
            android:fontFamily="@font/promptmedium"
            android:backgroundTint="@color/navy"
            app:cornerRadius="20dp"/>

    </LinearLayout>

</FrameLayout>